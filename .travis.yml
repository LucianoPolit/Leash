language: swift
osx_image: xcode9
podfile: Example/Podfile

before_install:
  - gem install cocoapods
  - pod repo update

script:
  - xcodebuild -workspace Example/Example.xcworkspace -scheme Example -sdk iphonesimulator -destination 'name=iPhone X' -enableCodeCoverage YES test | xcpretty

after_success:
  - bash <(curl -s https://codecov.io/bash) -J Leash
